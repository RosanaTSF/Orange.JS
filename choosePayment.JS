const priceTag = 100;
const choosePayment = 1;

if (choosePayment === 1) {
    console.log (priceTag - (priceTag * 0.1));
} else if (choosePayment === 2) {
    console.log (priceTag - (priceTag * 0,15));
} else if (choosePayment === 3) {
    console.log (priceTag - (priceTag / 2));
} else (choosePayment === 4) {
    console.log (priceTag - (priceTag * 0.1));
}