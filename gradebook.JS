const nota1 = 5;
const nota2 = 4;
const nota3 = 9;
const media = (nota1 + nota2 + nota3) / 3;

console.log(media);

if (media < 5) {
    console.log('nota vermelha');
} else if (media = 5 && media <= 7) {
    console.log('nota amarela');
} else {
    console.log('nota azul');

}

